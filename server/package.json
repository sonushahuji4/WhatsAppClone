{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "http-status-codes": "^2.2.0",
    "mongoose": "^6.6.5",
    "socket.io": "^4.5.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.3",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "debug": "tsc && set SLS_DEBUG=* && node --inspect node_modules/serverless/bin/serverless offline start --stage npr --apiKey api-key-here",
    "build": "tsc",
    "pretest": "node ./node_modules/tslint/bin/tslint -c tslint.json 'src/**/*.ts'",
    "test": "nyc mocha -r ts-node/register tests/**/*.spec.ts",
    "dev": "nodemon ./src/server.ts",
    "start": "node ./dist/server.js"
  }
}
